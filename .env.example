# Copy to .env.local and fill in values for local development.
# Vercel automatically injects production values from the dashboard.

# Optional: override the public site URL used by metadata + sitemap
# NEXT_PUBLIC_SITE_URL=https://manasacharya.com

# Optional: Formspree form ID, if a contact form is added later
# NEXT_PUBLIC_FORMSPREE_ID=
